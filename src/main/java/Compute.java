/**
 *Compute is the main class of this program, and interfaces with the CPU and GPU classes
 * to output a rough calculation on performance differences.
 * todo:    Enter CPU specs and allow them to be stored in memory
 *          Calculate a CPU score based off the specs
 */

public class Compute {
    public Compute() {

    }
}
