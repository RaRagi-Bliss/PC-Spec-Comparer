import java.util.Scanner;

/**
 *Compute is the main class of this program, and interfaces with the CPU and GPU classes
 * to output a rough calculation on performance differences.
 * todo:    Create a new CPU object and store object to memory
 *          Compare entered CPUs and display their differences
 *          Store all CPU objects created in the session and automatically sort them based on performance
 */

public class Compute {
    public static void  main(String args[]){
        Scanner sc = new Scanner(System.in);
    }

    /**
     * Compares two CPUs together
     */
    public void compare(){

    }
}
